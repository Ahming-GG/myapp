services:
  app:
    build: .
    expose:
      - "3000"
    depends_on:
      - redis

  redis:
    image: redis

  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
